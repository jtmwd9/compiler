I did the global option. Returns nothing if everything is alright. Print errors and ends if something is amiss

James Markus
Static Semantics

BUILD:
	make statSem
RUN:
	./statSem [filename]
		where filename is input file for testing. can also use <filename
CLEAN:
	make clean 
