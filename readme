I did the global option. Returns only output file name if everything is alright. Print errors and ends if something is amiss

James Markus
Compiler

BUILD:
	make compfs
RUN:
	./compfs [filename]
		where filename is input file for testing. can also use <filename
CLEAN:
	make clean 
